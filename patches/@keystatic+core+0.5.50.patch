diff --git a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.js b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.js
index aabdb1a..ff31e66 100644
--- a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.js
+++ b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.js
@@ -186,11 +186,11 @@ function makeGenericAPIRouteHandler(_config, options) {
 }
 const tokenDataResultType = s.type({
   access_token: s.string(),
-  expires_in: s.number(),
-  refresh_token: s.string(),
-  refresh_token_expires_in: s.number(),
-  scope: s.string(),
-  token_type: s.literal('bearer')
+  expires_in: s.optional(s.number()),
+  refresh_token: s.optional(s.string()),
+  refresh_token_expires_in: s.optional(s.number()),
+  scope: s.optional(s.string()),
+  token_type: s.optional(s.literal('bearer'))
 });
 async function githubOauthCallback(req, config) {
   var _req$headers$get2;
@@ -251,18 +251,21 @@ async function githubOauthCallback(req, config) {
   return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
 }
 async function getTokenCookies(tokenData, config) {
+  const expiresIn = tokenData.expires_in ?? 86400;
+  const refreshExpiresIn = tokenData.refresh_token_expires_in ?? 7776000;
+  const refreshToken = tokenData.refresh_token ?? tokenData.access_token;
   const headers = [['Set-Cookie', cookie.serialize('keystatic-gh-access-token', tokenData.access_token, {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
-    maxAge: tokenData.expires_in,
-    expires: new Date(Date.now() + tokenData.expires_in * 1000),
+    maxAge: expiresIn,
+    expires: new Date(Date.now() + expiresIn * 1000),
     path: '/'
-  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(tokenData.refresh_token, config.secret), {
+  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(refreshToken, config.secret), {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
     httpOnly: true,
-    maxAge: tokenData.refresh_token_expires_in,
-    expires: new Date(Date.now() + tokenData.refresh_token_expires_in * 100),
+    maxAge: refreshExpiresIn,
+    expires: new Date(Date.now() + refreshExpiresIn * 100),
     path: '/'
   })]];
   return headers;
@@ -270,16 +273,22 @@ async function getTokenCookies(tokenData, config) {
 async function getRefreshToken(req, config) {
   const cookies = cookie.parse(req.headers.get('cookie') || '');
   const refreshTokenCookie = cookies['keystatic-gh-refresh-token'];
-  if (!refreshTokenCookie) return;
-  let refreshToken;
-  try {
-    refreshToken = await decryptValue(refreshTokenCookie, config.secret);
-  } catch {
-    return;
+  if (refreshTokenCookie) {
+    try {
+      return await decryptValue(refreshTokenCookie, config.secret);
+    } catch {
+      // fall through to access token fallback
+    }
   }
-  return refreshToken;
+  return cookies['keystatic-gh-access-token'] || undefined;
 }
 async function githubRefreshToken(req, config) {
+  const cookies = cookie.parse(req.headers.get('cookie') || '');
+  const accessToken = cookies['keystatic-gh-access-token'];
+  if (accessToken) {
+    const headers = await getTokenCookies({ access_token: accessToken }, config);
+    return { status: 200, headers, body: '' };
+  }
   const headers = await refreshGitHubAuth(req, config);
   if (!headers) {
     return {
@@ -310,14 +319,14 @@ async function refreshGitHubAuth(req, config) {
     }
   });
   if (!tokenRes.ok) {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   const _tokenData = await tokenRes.json();
   let tokenData;
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   return getTokenCookies(tokenData, config);
 }
diff --git a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.js b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.js
index b4c82e2..974b9a2 100644
--- a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.js
+++ b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.js
@@ -389,11 +389,11 @@ function makeGenericAPIRouteHandler(_config, options) {
 }
 const tokenDataResultType = s.type({
   access_token: s.string(),
-  expires_in: s.number(),
-  refresh_token: s.string(),
-  refresh_token_expires_in: s.number(),
-  scope: s.string(),
-  token_type: s.literal('bearer')
+  expires_in: s.optional(s.number()),
+  refresh_token: s.optional(s.string()),
+  refresh_token_expires_in: s.optional(s.number()),
+  scope: s.optional(s.string()),
+  token_type: s.optional(s.literal('bearer'))
 });
 async function githubOauthCallback(req, config) {
   var _req$headers$get2;
@@ -454,18 +454,21 @@ async function githubOauthCallback(req, config) {
   return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
 }
 async function getTokenCookies(tokenData, config) {
+  const expiresIn = tokenData.expires_in ?? 86400;
+  const refreshExpiresIn = tokenData.refresh_token_expires_in ?? 7776000;
+  const refreshToken = tokenData.refresh_token ?? tokenData.access_token;
   const headers = [['Set-Cookie', cookie.serialize('keystatic-gh-access-token', tokenData.access_token, {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
-    maxAge: tokenData.expires_in,
-    expires: new Date(Date.now() + tokenData.expires_in * 1000),
+    maxAge: expiresIn,
+    expires: new Date(Date.now() + expiresIn * 1000),
     path: '/'
-  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(tokenData.refresh_token, config.secret), {
+  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(refreshToken, config.secret), {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
     httpOnly: true,
-    maxAge: tokenData.refresh_token_expires_in,
-    expires: new Date(Date.now() + tokenData.refresh_token_expires_in * 100),
+    maxAge: refreshExpiresIn,
+    expires: new Date(Date.now() + refreshExpiresIn * 100),
     path: '/'
   })]];
   return headers;
@@ -473,16 +476,22 @@ async function getTokenCookies(tokenData, config) {
 async function getRefreshToken(req, config) {
   const cookies = cookie.parse(req.headers.get('cookie') || '');
   const refreshTokenCookie = cookies['keystatic-gh-refresh-token'];
-  if (!refreshTokenCookie) return;
-  let refreshToken;
-  try {
-    refreshToken = await decryptValue(refreshTokenCookie, config.secret);
-  } catch {
-    return;
+  if (refreshTokenCookie) {
+    try {
+      return await decryptValue(refreshTokenCookie, config.secret);
+    } catch {
+      // fall through to access token fallback
+    }
   }
-  return refreshToken;
+  return cookies['keystatic-gh-access-token'] || undefined;
 }
 async function githubRefreshToken(req, config) {
+  const cookies = cookie.parse(req.headers.get('cookie') || '');
+  const accessToken = cookies['keystatic-gh-access-token'];
+  if (accessToken) {
+    const headers = await getTokenCookies({ access_token: accessToken }, config);
+    return { status: 200, headers, body: '' };
+  }
   const headers = await refreshGitHubAuth(req, config);
   if (!headers) {
     return {
@@ -513,14 +522,14 @@ async function refreshGitHubAuth(req, config) {
     }
   });
   if (!tokenRes.ok) {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   const _tokenData = await tokenRes.json();
   let tokenData;
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   return getTokenCookies(tokenData, config);
 }
@@ -539,6 +548,7 @@ async function githubLogin(req, config) {
   const url = new URL('https://github.com/login/oauth/authorize');
   url.searchParams.set('client_id', config.clientId);
   url.searchParams.set('redirect_uri', `${reqUrl.origin}/api/keystatic/github/oauth/callback`);
+  url.searchParams.set('scope', 'repo');
   if (from === '/') {
     return redirect(url.toString());
   }
diff --git a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.react-server.js b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.react-server.js
index fb21fd0..8f5dcbd 100644
--- a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.react-server.js
+++ b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.node.react-server.js
@@ -389,11 +389,11 @@ function makeGenericAPIRouteHandler(_config, options) {
 }
 const tokenDataResultType = s.type({
   access_token: s.string(),
-  expires_in: s.number(),
-  refresh_token: s.string(),
-  refresh_token_expires_in: s.number(),
-  scope: s.string(),
-  token_type: s.literal('bearer')
+  expires_in: s.optional(s.number()),
+  refresh_token: s.optional(s.string()),
+  refresh_token_expires_in: s.optional(s.number()),
+  scope: s.optional(s.string()),
+  token_type: s.optional(s.literal('bearer'))
 });
 async function githubOauthCallback(req, config) {
   var _req$headers$get2;
@@ -454,18 +454,21 @@ async function githubOauthCallback(req, config) {
   return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
 }
 async function getTokenCookies(tokenData, config) {
+  const expiresIn = tokenData.expires_in ?? 86400;
+  const refreshExpiresIn = tokenData.refresh_token_expires_in ?? 7776000;
+  const refreshToken = tokenData.refresh_token ?? tokenData.access_token;
   const headers = [['Set-Cookie', cookie.serialize('keystatic-gh-access-token', tokenData.access_token, {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
-    maxAge: tokenData.expires_in,
-    expires: new Date(Date.now() + tokenData.expires_in * 1000),
+    maxAge: expiresIn,
+    expires: new Date(Date.now() + expiresIn * 1000),
     path: '/'
-  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(tokenData.refresh_token, config.secret), {
+  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(refreshToken, config.secret), {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
     httpOnly: true,
-    maxAge: tokenData.refresh_token_expires_in,
-    expires: new Date(Date.now() + tokenData.refresh_token_expires_in * 100),
+    maxAge: refreshExpiresIn,
+    expires: new Date(Date.now() + refreshExpiresIn * 100),
     path: '/'
   })]];
   return headers;
@@ -473,16 +476,22 @@ async function getTokenCookies(tokenData, config) {
 async function getRefreshToken(req, config) {
   const cookies = cookie.parse(req.headers.get('cookie') || '');
   const refreshTokenCookie = cookies['keystatic-gh-refresh-token'];
-  if (!refreshTokenCookie) return;
-  let refreshToken;
-  try {
-    refreshToken = await decryptValue(refreshTokenCookie, config.secret);
-  } catch {
-    return;
+  if (refreshTokenCookie) {
+    try {
+      return await decryptValue(refreshTokenCookie, config.secret);
+    } catch {
+      // fall through to access token fallback
+    }
   }
-  return refreshToken;
+  return cookies['keystatic-gh-access-token'] || undefined;
 }
 async function githubRefreshToken(req, config) {
+  const cookies = cookie.parse(req.headers.get('cookie') || '');
+  const accessToken = cookies['keystatic-gh-access-token'];
+  if (accessToken) {
+    const headers = await getTokenCookies({ access_token: accessToken }, config);
+    return { status: 200, headers, body: '' };
+  }
   const headers = await refreshGitHubAuth(req, config);
   if (!headers) {
     return {
@@ -513,14 +522,14 @@ async function refreshGitHubAuth(req, config) {
     }
   });
   if (!tokenRes.ok) {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   const _tokenData = await tokenRes.json();
   let tokenData;
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   return getTokenCookies(tokenData, config);
 }
@@ -539,6 +548,7 @@ async function githubLogin(req, config) {
   const url = new URL('https://github.com/login/oauth/authorize');
   url.searchParams.set('client_id', config.clientId);
   url.searchParams.set('redirect_uri', `${reqUrl.origin}/api/keystatic/github/oauth/callback`);
+  url.searchParams.set('scope', 'repo');
   if (from === '/') {
     return redirect(url.toString());
   }
diff --git a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.react-server.js b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.react-server.js
index 2246207..72e0d70 100644
--- a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.react-server.js
+++ b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.react-server.js
@@ -205,11 +205,11 @@ function makeGenericAPIRouteHandler(_config, options) {
 }
 const tokenDataResultType = s.type({
   access_token: s.string(),
-  expires_in: s.number(),
-  refresh_token: s.string(),
-  refresh_token_expires_in: s.number(),
-  scope: s.string(),
-  token_type: s.literal('bearer')
+  expires_in: s.optional(s.number()),
+  refresh_token: s.optional(s.string()),
+  refresh_token_expires_in: s.optional(s.number()),
+  scope: s.optional(s.string()),
+  token_type: s.optional(s.literal('bearer'))
 });
 async function githubOauthCallback(req, config) {
   var _req$headers$get2;
@@ -270,18 +270,21 @@ async function githubOauthCallback(req, config) {
   return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
 }
 async function getTokenCookies(tokenData, config) {
+  const expiresIn = tokenData.expires_in ?? 86400;
+  const refreshExpiresIn = tokenData.refresh_token_expires_in ?? 7776000;
+  const refreshToken = tokenData.refresh_token ?? tokenData.access_token;
   const headers = [['Set-Cookie', cookie.serialize('keystatic-gh-access-token', tokenData.access_token, {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
-    maxAge: tokenData.expires_in,
-    expires: new Date(Date.now() + tokenData.expires_in * 1000),
+    maxAge: expiresIn,
+    expires: new Date(Date.now() + expiresIn * 1000),
     path: '/'
-  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(tokenData.refresh_token, config.secret), {
+  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(refreshToken, config.secret), {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
     httpOnly: true,
-    maxAge: tokenData.refresh_token_expires_in,
-    expires: new Date(Date.now() + tokenData.refresh_token_expires_in * 100),
+    maxAge: refreshExpiresIn,
+    expires: new Date(Date.now() + refreshExpiresIn * 100),
     path: '/'
   })]];
   return headers;
@@ -289,16 +292,22 @@ async function getTokenCookies(tokenData, config) {
 async function getRefreshToken(req, config) {
   const cookies = cookie.parse(req.headers.get('cookie') || '');
   const refreshTokenCookie = cookies['keystatic-gh-refresh-token'];
-  if (!refreshTokenCookie) return;
-  let refreshToken;
-  try {
-    refreshToken = await decryptValue(refreshTokenCookie, config.secret);
-  } catch {
-    return;
+  if (refreshTokenCookie) {
+    try {
+      return await decryptValue(refreshTokenCookie, config.secret);
+    } catch {
+      // fall through to access token fallback
+    }
   }
-  return refreshToken;
+  return cookies['keystatic-gh-access-token'] || undefined;
 }
 async function githubRefreshToken(req, config) {
+  const cookies = cookie.parse(req.headers.get('cookie') || '');
+  const accessToken = cookies['keystatic-gh-access-token'];
+  if (accessToken) {
+    const headers = await getTokenCookies({ access_token: accessToken }, config);
+    return { status: 200, headers, body: '' };
+  }
   const headers = await refreshGitHubAuth(req, config);
   if (!headers) {
     return {
@@ -329,14 +338,14 @@ async function refreshGitHubAuth(req, config) {
     }
   });
   if (!tokenRes.ok) {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   const _tokenData = await tokenRes.json();
   let tokenData;
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   return getTokenCookies(tokenData, config);
 }
@@ -355,6 +364,7 @@ async function githubLogin(req, config) {
   const url = new URL('https://github.com/login/oauth/authorize');
   url.searchParams.set('client_id', config.clientId);
   url.searchParams.set('redirect_uri', `${reqUrl.origin}/api/keystatic/github/oauth/callback`);
+  url.searchParams.set('scope', 'repo');
   if (from === '/') {
     return redirect(url.toString());
   }
diff --git a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.worker.js b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.worker.js
index 2246207..72e0d70 100644
--- a/node_modules/@keystatic/core/dist/keystatic-core-api-generic.worker.js
+++ b/node_modules/@keystatic/core/dist/keystatic-core-api-generic.worker.js
@@ -205,11 +205,11 @@ function makeGenericAPIRouteHandler(_config, options) {
 }
 const tokenDataResultType = s.type({
   access_token: s.string(),
-  expires_in: s.number(),
-  refresh_token: s.string(),
-  refresh_token_expires_in: s.number(),
-  scope: s.string(),
-  token_type: s.literal('bearer')
+  expires_in: s.optional(s.number()),
+  refresh_token: s.optional(s.string()),
+  refresh_token_expires_in: s.optional(s.number()),
+  scope: s.optional(s.string()),
+  token_type: s.optional(s.literal('bearer'))
 });
 async function githubOauthCallback(req, config) {
   var _req$headers$get2;
@@ -270,18 +270,21 @@ async function githubOauthCallback(req, config) {
   return redirect(`/keystatic${from ? `/${from}` : ''}`, headers);
 }
 async function getTokenCookies(tokenData, config) {
+  const expiresIn = tokenData.expires_in ?? 86400;
+  const refreshExpiresIn = tokenData.refresh_token_expires_in ?? 7776000;
+  const refreshToken = tokenData.refresh_token ?? tokenData.access_token;
   const headers = [['Set-Cookie', cookie.serialize('keystatic-gh-access-token', tokenData.access_token, {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
-    maxAge: tokenData.expires_in,
-    expires: new Date(Date.now() + tokenData.expires_in * 1000),
+    maxAge: expiresIn,
+    expires: new Date(Date.now() + expiresIn * 1000),
     path: '/'
-  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(tokenData.refresh_token, config.secret), {
+  })], ['Set-Cookie', cookie.serialize('keystatic-gh-refresh-token', await encryptValue(refreshToken, config.secret), {
     sameSite: 'lax',
     secure: process.env.NODE_ENV === 'production',
     httpOnly: true,
-    maxAge: tokenData.refresh_token_expires_in,
-    expires: new Date(Date.now() + tokenData.refresh_token_expires_in * 100),
+    maxAge: refreshExpiresIn,
+    expires: new Date(Date.now() + refreshExpiresIn * 100),
     path: '/'
   })]];
   return headers;
@@ -289,16 +292,22 @@ async function getTokenCookies(tokenData, config) {
 async function getRefreshToken(req, config) {
   const cookies = cookie.parse(req.headers.get('cookie') || '');
   const refreshTokenCookie = cookies['keystatic-gh-refresh-token'];
-  if (!refreshTokenCookie) return;
-  let refreshToken;
-  try {
-    refreshToken = await decryptValue(refreshTokenCookie, config.secret);
-  } catch {
-    return;
+  if (refreshTokenCookie) {
+    try {
+      return await decryptValue(refreshTokenCookie, config.secret);
+    } catch {
+      // fall through to access token fallback
+    }
   }
-  return refreshToken;
+  return cookies['keystatic-gh-access-token'] || undefined;
 }
 async function githubRefreshToken(req, config) {
+  const cookies = cookie.parse(req.headers.get('cookie') || '');
+  const accessToken = cookies['keystatic-gh-access-token'];
+  if (accessToken) {
+    const headers = await getTokenCookies({ access_token: accessToken }, config);
+    return { status: 200, headers, body: '' };
+  }
   const headers = await refreshGitHubAuth(req, config);
   if (!headers) {
     return {
@@ -329,14 +338,14 @@ async function refreshGitHubAuth(req, config) {
     }
   });
   if (!tokenRes.ok) {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   const _tokenData = await tokenRes.json();
   let tokenData;
   try {
     tokenData = tokenDataResultType.create(_tokenData);
   } catch {
-    return;
+    return getTokenCookies({ access_token: refreshToken }, config);
   }
   return getTokenCookies(tokenData, config);
 }
@@ -355,6 +364,7 @@ async function githubLogin(req, config) {
   const url = new URL('https://github.com/login/oauth/authorize');
   url.searchParams.set('client_id', config.clientId);
   url.searchParams.set('redirect_uri', `${reqUrl.origin}/api/keystatic/github/oauth/callback`);
+  url.searchParams.set('scope', 'repo');
   if (from === '/') {
     return redirect(url.toString());
   }

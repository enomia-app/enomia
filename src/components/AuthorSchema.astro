---
import { marcChenut, livreMethode97 } from '../data/author';

const ld = {
  '@context': 'https://schema.org',
  '@graph': [marcChenut, livreMethode97],
};
---
<script type="application/ld+json" set:html={JSON.stringify(ld)} />
